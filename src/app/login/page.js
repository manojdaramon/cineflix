import Login from "@/resources/components/login-page";

export const metadata = {
    title: "Sign In - Cineflix",
    description: "Sign in to your Cineflix account",
};

export default function LoginPage() {
    return <Login />;
}
